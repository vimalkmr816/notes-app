import { createUseStyles } from "react-jss";
export default createUseStyles ( {
	rootContainer: {
		display        : "flex",
		alignItems     : "center",
		justifyContent : "space-evenly",
		margin         : "0.7rem 0"
	}
} );
